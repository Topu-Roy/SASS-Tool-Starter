---
import Layout from "../../layouts/Layout.astro";
import PageRenderer from "../../components/PageRenderer.astro";
import { getContent } from "../../i18n";
const content = getContent(Astro.currentLocale || "ja")!;
---
<Layout page="home" title={content.home.title} description={content.layout.description}>
  <PageRenderer page="home" content={content.home} />
</Layout>
